[pytest]
asyncio_mode = auto
# Session-scoped loop: app.database.engine is a module-level singleton whose
# connection pool must not be split across per-test event loops.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
